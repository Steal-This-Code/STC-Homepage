# STC Homepage

StealThisCode.org homepage and static project pages.

Included starters:

- `tidycops.html` for the tidycops package landing page
- `neighborhood-watch.html` for a static-first neighborhood safety app blueprint
- `.github/workflows/nightly-neighborhood-watch-data.yml` for a nightly GitHub Actions data build
- `scripts/build_neighborhood_watch_data.R` for generating static data artifacts with `tidycops`
